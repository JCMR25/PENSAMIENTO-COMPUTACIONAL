#include <iostream>
using namespace std;
int main() {
    int numero;
    cout<<"Ingresa un número: ";
    cin>>numero;
    string R = (numero % 2 == 0) ? "par" : "impar";
    cout<<"El número es "<<R<<endl;

    return 0;

}
